# سوالات و پاسخ‌های امتحانی فصل ۳: آرایه، لیست پیوندی، پشته، صف
## درس: ساختمان داده‌ها - تألیف یوسفی

---

## بخش اول: آرایه (Array)

### سوال ۱) آرایه چیست و چه ویژگی‌هایی دارد؟

**پاسخ:**
- آرایه از تعدادی **عنصر همنوع و هم‌اندازه** تشکیل شده که به صورت **متوالی** در حافظه ذخیره می‌شود.
- آرایه یک **ساختار داده خطی** است.
- تعداد عناصر: `U - L + 1`
- حافظه تخصیصی: `(U - L + 1) * n` بایت (n = sizeof(Type))

---

### سوال ۲) آدرس شروع عنصر A[i] در آرایه A[L..U] را بنویسید.

**پاسخ:**
```
آدرس A[i] = (i - L) * n + α
```
که در آن:
- `n` = اندازه هر عنصر (بایت)
- `α` = آدرس شروع آرایه در حافظه

---

### سوال ۳) در زبان C تعریف `float f[100]` چه مقدار حافظه اشغال می‌کند؟ اگر آدرس شروع `α = 1000` باشد آدرس `A[20]` چقدر است؟

**پاسخ:**
- حافظه: `100 × 4 = 400 بایت` (چون `sizeof(float) = 4`)
- عنصر `A[20]` عنصر ۲۱ام آرایه است (چون اندیس از ۰ شروع می‌شود)
- آدرس `A[20] = (20 - 0) × 4 + 1000 = 1080`

---

### سوال ۴) آرایه چندبُعدی به چه دو طریق در حافظه ذخیره می‌شود؟ فرمول آدرس عنصر `A[i,j]` در هر روش را بنویسید.

**پاسخ:**

**الف) روش سطری (Row Major):**
```
آدرس A[i,j] = [(i - L₁)(U₂ - L₂ + 1) + (j - L₂)] × n + α
```

**ب) روش ستونی (Column Major):**
```
آدرس A[i,j] = [(j - L₂)(U₁ - L₁ + 1) + (i - L₁)] × n + α
```

---

### سوال ۵) در آرایه ۴ بُعدی `float f[5][10][15][20]` عنصر `f[2][3][4][6]` در روش سطری عنصر چندم است؟

**پاسخ:**
```
[(2-0)(10)(15)(20) + (3-0)(15)(20) + (4-0)(20) + (6-0)] + 1
= [6000 + 900 + 80 + 6] + 1 = 6987
```
یعنی عنصر **۶۹۸۷ام** است.

---

### سوال ۶) آدرس شروع عنصر `f[2][3][4][6]` در روش سطری با α=1000 و n=4 چقدر است؟

**پاسخ:**
```
[(2-0)(10)(15)(20) + (3-0)(15)(20) + (4-0)(20) + (6-0)] × 4 + 1000
= [6000 + 900 + 80 + 6] × 4 + 1000 = 6986 × 4 + 1000 = 28944
```
آدرس شروع = **۲۸۹۴۴**

---

### سوال ۷) ماتریس مثلثی پایین چیست؟ فرمول مکان عنصر aij در ذخیره سطری بنویسید.

**پاسخ:**
ماتریس مثلثی پایین ماتریسی است که تمام عناصر بالای قطر اصلی صفر هستند.
```
مکان aij در B = i(i-1)/2 + j
```
مثال: مکان a₃₂ = 3(3-1)/2 + 2 = 3 + 2 = **۵**

---

### سوال ۸) اگر ماتریس مثلثی پایین به صورت ستونی ذخیره شود فرمول مکان aij چیست؟

**پاسخ:**
```
مکان aij = (2n - 2 + 2)(j - 1)/2 + i - j + 1
         = n(j-1) + i - j + 1
```
مثال: مکان a₃₂ = n + 2

---

### سوال ۹) عناصر غیرصفر ماتریس ۳ قطری An×n را به صورت سطری در آرایه B[1..3n-2] ذخیره کنید. فرمول مکان aij را بنویسید.

**پاسخ:**
```
مکان aij = 2i + j - 2
```
مثال: مکان a₃₂ = 2(3) + 2 - 2 = **۶**

---

### سوال ۱۰) الگوریتم جستجوی خطی و دودویی را مقایسه کنید.

**پاسخ:**

| ویژگی | جستجوی خطی | جستجوی دودویی |
|--------|------------|----------------|
| شرط | آرایه نامرتّب | آرایه مرتّب صعودی |
| حداقل مقایسه | ۱ | ۱ |
| حداکثر مقایسه | n | ⌊lg n⌋ + 1 |
| متوسط مقایسه | (n+1)/2 | ⌊lg(n+1)⌋ |
| مرتّب زمانی | O(n) | O(lg n) |

---

## بخش دوم: لیست پیوندی (Linked List)

### سوال ۱۱) لیست پیوندی چیست و چه مزیتی نسبت به آرایه دارد؟

**پاسخ:**
- لیست پیوندی ساختار داده‌ای است که عناصر آن به صورت خطی هستند ولی **ترتیب خطی با اشاره‌گرها** مشخص می‌شود (نه اندیس).
- **مزیت:** اگر تعداد عناصر از قبل مشخص نباشد لیست پیوندی بهتر است.
- **مزیت:** برای عملیات حذف و درج لیست پیوندی بهتر است.
- انواع: یک‌طرفه، دوطرفه، حلقه‌ای و غیرحلقه‌ای

---

### سوال ۱۲) تعریف نود لیست یک‌طرفه را به زبان C و پاسکال بنویسید.

**پاسخ:**

**زبان C:**
```c
struct node {
    items data;
    node *next;
};
```

**پاسکال:**
```pascal
type
    node = record
        data : items;
        next : ^node;
    end;
```

---

### سوال ۱۳) الگوریتم جستجو در لیست پیوندی را بنویسید و مرتّب آن را مشخص کنید.

**پاسخ:**
```
search(k)
{
    x = head
    while (x ≠ nil and data(x) ≠ k)
        x = next(x)
    return (x)
}
```
**مرتّب:** O(n) - برای لیست n گرهی

---

### سوال ۱۴) درج نود بعد از نود y با آدرس مشخص در لیست یک‌طرفه چگونه است؟

**پاسخ:**
```
1. new(x)
2. next(x) = next(y)
3. next(y) = x
```
**مرتّب:** θ(1) - ثابت

---

### سوال ۱۵) حذف نود y از لیست پیوندی چگونه است؟

**پاسخ:**
```
delete(y)
{
    x = head
    while (next(x) ≠ y)
        x = next(x)
    next(x) = next(y)
    dispose(y)
}
```
**مرتّب:** O(n) - چون باید لیست پیمایش شود تا به نود قبل از y برسیم

---

### سوال ۱۶) در لیست پیوندی حلقه‌ای چه تفاوتی با لیست خطی وجود دارد؟

**پاسخ:**
- در لیست خطی اشاره‌گر آخرین نود **nil** است
- در لیست حلقه‌ای اشاره‌گر آخرین نود به **نود اول** اشاره می‌کند
- در لیست حلقه‌ای می‌توان از **هر نودی** لیست را پیمایش کرد
- در الگوریتم‌ها بجای مقایسه با nil باید با **head** مقایسه شود

---

### سوال ۱۷) چه مقدار آدرس در عملیات زیر تغییر می‌کند؟
- الف) حذف یک عنصر از لیست یک‌طرفه با وجود Avail
- ب) اضافه کردن یک عنصر به لیست یک‌طرفه با وجود Avail
- ج) حذف یک عنصر از لیست دوطرفه

**پاسخ:**
- الف) **۳ آدرس** تغییر می‌کند
- ب) **۳ آدرس** تغییر می‌کند
- ج) **۲ آدرس** تغییر می‌کند

---

### سوال ۱۸) لیست Avail چیست و چه کاربردی دارد؟

**پاسخ:**
- لیست Avail یا لیست در دسترس حافظه، لیست حاوی **گره‌های بلااستفاده** است
- اشاره‌گر Avail به اولین گره آن اشاره می‌کند
- هر گره‌ای که از لیست اصلی حذف شود به لیست Avail اضافه می‌شود
- برای اضافه کردن به لیست اصلی از گره‌های لیست Avail استفاده می‌شود

---

### سوال ۱۹) الگوریتم اضافه کردن گره به ابتدای لیست اصلی با وجود Avail را بنویسید.

**پاسخ:**
```
1. temp := Avail
2. Avail := Avail^.Link
3. temp^.Link := head
4. head := temp
```

---

### سوال ۲۰) اگر لیست Avail خالی باشد (Avail=Null) و بخواهیم عنصر جدیدی اضافه کنیم چه اتفاقی می‌افتد؟

**پاسخ:**
**سرریزی (Overflow)** رخ می‌دهد - یعنی نمی‌توانیم گره‌ای به لیست اضافه کنیم.

---

## بخش سوم: پشته (Stack)

### سوال ۲۱) پشته چیست و چه سیاستی را پیاده‌سازی می‌کند؟

**پاسخ:**
- پشته مجموعه‌ای دینامیکی است که عناصر به ترتیب از پیش تعریف شده حذف و درج می‌شوند
- عنصری که حذف می‌شود **آخرین عنصری** است که درج شده
- پشته سیاست **LIFO** (Last In First Out) را پیاده‌سازی می‌کند
- عمل اضافه کردن: **push**
- عمل حذف: **pop**

---

### سوال ۲۲) الگوریتم push و pop پشته با آرایه را بنویسید.

**پاسخ:**

**push(s, x):**
```
if (top == n)
    error "over flow"
else
    top = top + 1
    s[top] = x
```

**pop(s):**
```
if (top == 0)
    error "under flow"
else
    top = top - 1
    return s[top + 1]
```

هر دو تابع از مرتبه **θ(1)** هستند.

---

### سوال ۲۳) اعداد ۱ تا ۵ به ترتیب صعودی ورودی داده شوند و push شوند. کدام خروجی قابل تولید نیست؟
- الف) ۳ ۲ ۴ ۵ ۱
- ب) ۳ ۴ ۲ ۵ ۱
- ج) ۱ ۲ ۵ ۴ ۳
- د) ۳ ۴ ۱ ۵ ۲

**پاسخ:**
**گزینه د) ۳ ۴ ۱ ۵ ۲** قابل تولید نیست.

زیرا بعد از push کردن عدد ۴، عدد ۱ قابل pop شدن نیست (چون قبلاً pop نشده).

**نکته:** اگر در خروجی دنباله‌ای مثل `...x₁...x₂...x₃...` وجود داشته باشد که `x₂ < x₃ < x₁` باشد، آن دنباله قابل تولید نیست.

---

### سوال ۲۴) با n عدد ورودی تعداد دنباله‌های قابل تولید از پشته چقدر است؟

**پاسخ:**
$$\frac{1}{n+1}\binom{2n}{n}$$
(عدد کاتالان)

مثال: با ۳ عدد (1,2,3) تعداد = $\frac{1}{4}\binom{6}{3} = 5$
دنباله‌ها: 123, 132, 213, 231, 321
(دنباله 312 قابل تولید نیست)

---

### سوال ۲۵) پشته‌های چندگانه در یک آرایه چگونه پیاده‌سازی می‌شوند؟

**پاسخ:**
- اگر دو پشته باشد: از دو متغیر **top1** و **top2** که در جهت عکس هم حرکت می‌کنند استفاده می‌شود
- `S[1]` ابتدای پشته اول و `S[n]` ابتدای پشته دوم
- شرط خالی بودن پشته اول: `top1 = 0`
- شرط خالی بودن پشته دوم: `top2 = n+1`
- شرط پر بودن: `top2 = top1 + 1`

---

### سوال ۲۶) اگر بیش از دو پشته در آرایه S[1..n] بسازیم چگونه عمل می‌کنیم؟

**پاسخ:**
- آرایه را **m قسمت** می‌کنیم
- اندازه هر قسمت تقریباً `⌊n/m⌋` است
- برای هر پشته `i`:
  - `b[i]` = یکی کمتر از پایین‌ترین عنصر
  - `t[i]` = بالاترین عنصر
  - مقادیر اولیه: `b[i] = t[i] = ⌊n/m⌋(i-1)`
- شرط خالی بودن پشته `i`: `b[i] = t[i]`
- شرط پر بودن: `t[i] = b[i+1]`

---

### سوال ۲۷) پشته با لیست پیوندی چگونه پیاده‌سازی می‌شود؟

**پاسخ:**
```
push(item):
    temp = new node
    if (temp ≠ 0)
        temp.data = item
        temp.link = top
        top = temp
        return true
    else
        write "out of space"
        return false

pop(item):
    if (top == 0)
        write "stack is empty"
        return false
    else
        item = top.data
        temp = top
        top = top.link
        delete temp
        return true
```

---

### سوال ۲۸) یکی از کاربردهای پشته چیست؟ الگوریتم آن را توضیح دهید.

**پاسخ:**
**جمع دو عدد بزرگ:**
- ارقام دو عدد را به دو پشته وارد می‌کنیم
- تا زمانی که حداقل یکی از پشته‌ها خالی نشده:
  - ارقام را از دو پشته pop کرده جمع می‌کنیم
  - حاصل جمع را به پشته دیگر وارد می‌کنیم
  - رقم نقلی را در متغیری ذخیره می‌کنیم

مثال: جمع ۵۹۲ و ۳۷۸۴

---

## بخش چهارم: صف (Queue)

### سوال ۲۹) صف چیست و چه سیاستی را پیاده‌سازی می‌کند؟

**پاسخ:**
- صف لیست مرتبی است که عمل اضافه کردن **(Enqueue)** از یک طرف (انتها - rear) و عمل حذف **(Dequeue)** از طرف دیگر (ابتداء - front) انجام می‌شود
- صف سیاست **FIFO** (First In First Out) را پیاده‌سازی می‌کند
- یعنی اولین عنصر ورودی، اولین عنصر خروجی است
- بیشترین کاربرد صف در **زمان‌بندی برنامه‌ها** است

---

### سوال ۳۰) الگوریتم درج و حذف صف خطی را بنویسید.

**پاسخ:**

**addq(x):**
```
if rear = n then
    queuefull
else begin
    rear := rear + 1
    q[rear] := x
end
```

**delq(x):**
```
if front = rear then
    queueempty
else begin
    front := front + 1
    x := q[front]
end
```

**مشکل:** در صف خطی آرایه فقط یک‌بار قابل استفاده است.

---

### سوال ۳۱) صف حلقه‌ای (دوار) چیست و شرط پر بودن آن چیست؟

**پاسخ:**
- صف حلقه‌ای برای رفع مشکل صف خطی استفاده می‌شود
- وقتی `rear = n-1` شد عنصر بعدی در `q[0]` قرار می‌گیرد
- **شرط پر بودن:** `front = (rear + 1) mod n`
- همیشه یک خانه خالی می‌گذاریم تا پر و خالی بودن با هم اشتباه نشوند

---

### سوال ۳۲) الگوریتم درج و حذف صف حلقه‌ای را بنویسید.

**پاسخ:**

**addq(x):**
```
if front = (rear + 1) mod n then
    queuefull
else begin
    rear := (rear + 1) mod n
    q[rear] := x
end
```

**delq(x):**
```
if front = rear then
    queueempty
else begin
    front := (front + 1) mod n
    x := q[front]
end
```

---

### سوال ۳۳) تعداد خانه‌های پر در صف حلقه‌ای n عنصری چقدر است؟

**پاسخ:**
```
تعداد خانه‌های پر = (n - f + r) mod n
```
یعنی:
```
r - f          اگر f ≤ r
n - (f - r)    اگر f > r
```

---

## بخش پنجم: ارزیابی عبارات (Expression Evaluation)

### سوال ۳۴) سه فرم نوشتن عبارات ریاضی را نام ببرید.

**پاسخ:**
1. **میان‌نوردی (Infix):** عامل بین عملوندها → `a + b`
2. **پسوندی (Postfix/RPN):** عامل بعد از عملوندها → `ab +`
3. **پیشوندی (Prefix/Polish):** عامل قبل از عملوندها → `+ a b`

---

### سوال ۳۵) عبارت `a + b * c ^ a - b / c` را به فرم پسوندی و پیشوندی تبدیل کنید.

**پاسخ:**

**پسوندی:** `abca^*+bc/-`
**پیشوندی:** `-+a*b^ca/bc`

---

### سوال ۳۶) الگوریتم تبدیل عبارت میان‌نوردی به پسوندی با استفاده از پشته را توضیح دهید.

**پاسخ:**
1. عبارت infix را از چپ به راست پیمایش کنید
2. پرانتزهای باز را به پشته push کنید
3. عملوندها را در خروجی بنویسید
4. اگر به عاملگر رسیدید و روی پشته عاملگری بود که اولویت آن بیشتر یا مساوی عاملگر جدید بود، ابتدا آن را pop کنید در خروجی بنویسید و سپس عاملگر جدید را push کنید
5. اگر به پرانتز بسته رسیدید، عاملگرهای بالای پشته را تا پرانتز باز بردارید در خروجی بنویسید و پرانتز باز را حذف کنید (ولی در خروجی ننویسید)

---

### سوال ۳۷) اولویت عاملگرها چگونه است؟

**پاسخ:**
```
اولویت ۱: { } [ ] ( ) -1 (پرانتز)
اولویت ۲: not ! ~ ^ (توان) - از راست به چپ
اولویت ۳: * / div mod % -3
اولویت ۴: + -
```

---

## بخش ششم: ماتریس اسپارس (Sparse Matrix)

### سوال ۳۸) ماتریس اسپارس چیست و چگونه نمایش داده می‌شود؟

**پاسخ:**
ماتریسی که تعداد **صفرهای آن زیاد** است.

**نمایش:** از آرایه دوبعدی `Sparse[0..t, 0..2]` استفاده می‌شود:
- `t` = تعداد عناصر غیرصفر
- سطر اول: تعداد سطرها، تعداد ستون‌ها، تعداد عناصر غیرصفر
- سطرهای بعدی: شماره سطر، شماره ستون، مقدار عنصر

---

### سوال ۳۹) الگوریتم ترانهاده کردن ماتریس اسپارس را توضیح دهید.

**پاسخ:**

**روش ساده:** عناصر مکان (i,j) به مکان (j,i) منتقل شوند.
- **مرتّب زمانی:** O(nt)

**روش fasttranspose:** ابتدا تعداد عناصر را در هر ستون مشخص می‌کند.
- **مرتّب زمانی:** O(n + t)

---

## بخش هفتم: نمایش چندجمله‌ای‌ها

### سوال ۴۰) چندجمله‌ای p(x) = anx^n + ... + a0 را چگونه با آرایه نمایش می‌دهیم؟

**پاسخ:**

**روش اول:** ضرایب در آرایه یک‌بُعدی
مثال: `5x⁴ + 2x² - 6x + 3` → `[3, -6, 0, 2, 0, 5]`

**روش دوم:** ماتریس دو سطری (ضرایب و توان‌ها)
مناسب برای چندجمله‌ای‌هایی مانند `7x¹⁰⁰ - 3x + 4`

---

## بخش هشتم: تطبیق الگو (String Matching)

### سوال ۴۱) الگوریتم تطبیق الگوی KMP چیست و مرتّب آن چقدر است؟

**پاسخ:**
- الگوریتم KMP توسط **Knuth, Morris, Pratt** ارائه شده
- مرتّب زمانی: **O(n + m)** که m طول رشته S و n طول رشته P است
- از **تابع شکست (failure function)** استفاده می‌کند تا مقایسه‌های غیرضروری را حذف کند

---

## بخش نهم: مسائل ترکیبی و پیشرفته

### سوال ۴۲) الگوریتم مسئله مسیر پرپیچ‌وخم (MAZE) را توضیح دهید.

**پاسخ:**
- یک موش وارد جعبه بزرگ و رو باز می‌شود
- مسیرها با آرایه دوبعدی با مقادیر ۰ و ۱ نشان داده می‌شود
- صفر = راه باز، یک = راه بسته
- موش از `Maze[1,1]` وارد و از `Maze[m,p]` خارج می‌شود
- از **پشته** برای یافتن مسیر استفاده می‌شود
- انداره پشته باید `mp` باشد
- مرتّب زمانی: O(mp)

---

### سوال ۴۳) صف دوسر (Double Ended Queue) چیست؟

**پاسخ:**
- صفی که حذف و درج را از **هر دو طرف** اجازه می‌دهد
- برخلاف پشته (فقط یک طرف) و صف معمولی (حذف از یک طرف، درج از طرف دیگر)

---

### سوال ۴۴) با دو پشته یک صف بسازید. هزینه n عمل O(n) باشد.

**پاسخ:**
- از یک پشته برای درج و از پشته دیگر برای حذف استفاده می‌شود
- هنگام حذف از پشته دوم (اگر خالی باشد) عناصر پشته اول را به پشته دوم منتقل می‌کنیم

---

### سوال ۴۵) با دو صف یک پشته بسازید.

**پاسخ:**
- از یک صف برای نگهداری عناصر و از صف دیگر به عنوان کمکی استفاده می‌شود
- هنگام push، عنصر جدید را به صف اول اضافه و عناصر صف اول را یکی‌یکی به صف دوم منتقل می‌کنیم و سپس صفها را عوض می‌کنیم

---

## جدول خلاصه مرتّب زمانی عملیات

| عملیات | آرایه | لیست پیوندی یک‌طرفه | پشته (آرایه) | صف حلقه‌ای |
|--------|-------|---------------------|--------------|------------|
| دسترسی | O(1) | O(n) | - | - |
| جستجو | O(n) | O(n) | - | - |
| درج | O(n) | O(1)* | O(1) | O(1) |
| حذف | O(n) | O(1)** | O(1) | O(1) |

*در ابتدای لیست
**با داشتن آدرس نود قبلی

---

## نکات مهم امتحانی

1. **آرایه:** ساختار داده خطی، دسترسی تصادفی O(1)
2. **لیست پیوندی:** مناسب برای درج/حذف مکرر
3. **پشته:** LIFO - فقط از یک طرف
4. **صف:** FIFO - حذف از یک طرف، درج از طرف دیگر
5. **پشته حلقه‌ای:** شرط پر بودن = `(rear+1) mod n = front`
6. **تعداد دنباله‌های قابل تولید:** عدد کاتالان
7. **ترانهاده ماتریس اسپارس:** O(nt) ساده، O(n+t) سریع
