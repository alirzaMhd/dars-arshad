# پاسخ‌نامه فصل ۳: آرایه، لیست پیوندی، پشته، صف
## درس: ساختمان داده‌ها

---

## پاسخ‌های کوتاه (Quick Answers)

| سوال | پاسخ |
|------|------|
| ۱ | آرایه: عناصر همنوع، متوالی در حافظه، ساختار خطی |
| ۲ | `(i-L)*n + α` |
| ۳ | ۴۰۰ بایت، آدرس ۱۰۸۰ |
| ۴ | سطری: `[(i-L₁)(U₂-L₂+1)+(j-L₂)]*n+α` |
| ۵ | عنصر ۶۹۸۷ام |
| ۶ | آدرس ۲۸۹۴۴ |
| ۷ | `i(i-1)/2 + j` |
| ۸ | `n(j-1) + i - j + 1` |
| ۹ | `2i + j - 2` |
| ۱۰ | خطی: O(n)، دودویی: O(lg n) |
| ۱۱ | ساختار خطی با اشاره‌گر، مناسب درج/حذف |
| ۱۲ | struct node { items data; node *next; } |
| ۱۳ | O(n) - پیمایش خطی |
| ۱۴ | θ(1) - فقط تغییر اشاره‌گرها |
| ۱۵ | O(n) - نیاز به پیمایش برای یافتن نود قبلی |
| ۱۶ | اشاره‌گر آخر به اول وصل است نه nil |
| ۱۷ | الف) ۳، ب) ۳، ج) ۲ |
| ۱۸ | لیست گره‌های بلااستفاده حافظه |
| ۱۹ | ۴ دستور: temp←Avail, Avail←Avail.Link, temp.Link←head, head←temp |
| ۲۰ | سرریزی (Overflow) |
| ۲۱ | LIFO - آخرین ورودی اولین خروجی |
| ۲۲ | push: top++، pop: top-- هر دو θ(1) |
| ۲۳ | گزینه د) ۳ ۴ ۱ ۵ ۲ |
| ۲۴ | `1/(n+1) * C(2n,n)` عدد کاتالان |
| ۲۵ | top1 از چپ، top2 از راست |
| ۲۶ | تقسیم آرایه به m قسمت، ⌊n/m⌋ |
| ۲۷ | push=درج اول، pop=حذف اول |
| ۲۸ | جمع دو عدد بزرگ با دو پشته |
| ۲۹ | FIFO - اولین ورودی اولین خروجی |
| ۳۰ | addq: rear++، delq: front++ |
| ۳۱ | `(rear+1) mod n = front` |
| ۳۲ | rear:=(rear+1) mod n، front:=(front+1) mod n |
| ۳۳ | `(n-f+r) mod n` |
| ۳۴ | میان‌نوردی، پسوندی، پیشوندی |
| ۳۵ | پسوندی: abca^*+bc/-، پیشوندی: -+a*b^ca/bc |
| ۳۶ | push پرانتز باز، pop با اولویت ≥ |
| ۳۷ | ۱: پرانتز، ۲: not^، ۳: */، ۴: +- |
| ۳۸ | ماتریس با صفرهای زیاد، آرایه [0..t,0..2] |
| ۳۹ | ساده: O(nt)، سریع: O(n+t) |
| ۴۰ | آرایه ضرایب یا ماتریس دو سطری |
| ۴۱ | O(n+m) با تابع شکست |
| ۴۲ | با پشته، O(mp) |
| ۴۳ | حذف و درج از هر دو طرف |
| ۴۴ | دو پشته، یکی برای درج یکی برای حذف |
| ۴۵ | دو صف، یکی اصلی یکی کمکی |

---

## فرمول‌های کلیدی

### آرایه
```
آدرس A[i] = (i-L) * n + α
آدرس A[i,j] سطری = [(i-L₁)(U₂-L₂+1)+(j-L₂)] * n + α
آدرس A[i,j] ستونی = [(j-L₂)(U₁-L₁+1)+(i-L₁)] * n + α
```

### ماتریس مثلثی
```
پایین مثلثی سطری: i(i-1)/2 + j
پایین مثلثی ستونی: n(j-1) + i - j + 1
۳ قطری سطری: 2i + j - 2
۳ قطری ستونی: 2j + i - 2
```

### پشته
```
شرط پر: top = n
شرط خالی: top = 0
تعداد دنباله‌ها: C(2n,n)/(n+1)
```

### صف حلقه‌ای
```
شرط پر: front = (rear+1) mod n
شرط خالی: front = rear
تعداد پر: (n-f+r) mod n
```

### جستجو
```
خطی: O(n)
دودویی: O(lg n)
KMP: O(n+m)
```

### ماتریس اسپارس
```
ترانهاده ساده: O(nt)
ترانهاده سریع: O(n+t)
```

---

## نکات طلایی امتحان

1. **آرایه:** دسترسی تصادفی O(1)، درج/حذف O(n)
2. **لیست پیوندی:** درج/حذف O(1) اگر آدرس مشخص باشد
3. **پشته:** فقط push و pop، هر دو θ(1)
4. **صف خطی:** فقط یک‌بار قابل استفاده
5. **صف حلقه‌ای:** شرط پر ≠ شرط خالی با یک خانه خالی
6. **دنباله‌های پشته:** اگر x₂ < x₃ < x₁ در خروجی باشد غیرممکن است
7. **پشته با لیست:** push=درج اول، pop=حذف اول
8. **ترانهاده:** تعداد عناصر غیرصفر t مهم است
9. **اولویت عاملگرها:** پرانتز > not^ > */ > +-
